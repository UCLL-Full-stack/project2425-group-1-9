test("Given active cart; When calling createNewActiveCartAndDeactivateTheCurrentOne; Then active cart is deactivated and customer receives a new empty cart.", () => {
    // GIVEN

    // WHEN

    // THEN
});

test("Given customer's username; When calling getTotalCartPriceByCustomerUsername; Then total price of customer's active cart is returned.", () => {
    // GIVEN

    // WHEN

    // THEN
});